package com.example.shoppingassistant.feature.pages.di

import com.example.shoppingassistant.feature.pages.subscriptions.SubscriptionsViewModel
import org.koin.core.module.dsl.viewModel
import org.koin.dsl.module

val subscriptionsModule = module {
    viewModel {
        SubscriptionsViewModel(
            authRepository = get(),
            templateSubscriptionsRepository = get(),
            inboxRemote = get(),
            settingsStorage = get(),
            pushScheduler = get(),
            catalogRepository = get(),
            attributeService = get(),
            constraintsResolver = get(),
            suggestEngine = get(),
            templateIdTask = get(),
        )
    }
}